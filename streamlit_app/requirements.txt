# Pinned to what the committed models/*.joblib were trained & pickled
# with (see train_models.py) — unpickling a scikit-learn/imblearn model
# with a mismatched version can silently misbehave or fail to load, so
# don't loosen these without re-running train_models.py against the new
# versions and re-committing the artifacts.
streamlit==1.61.1
pandas==3.0.1
numpy==2.4.2
scikit-learn==1.8.0
imbalanced-learn==0.14.2
joblib==1.5.3
altair==6.2.2
